---
name: 🔍 Testing

on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
    paths-ignore:
      - README.md

jobs:

  harvest-test-linux:
    uses: newrelic/infrastructure-agent/.github/workflows/component_linux_harvest_test.yml@flaky/emulator_TEST

